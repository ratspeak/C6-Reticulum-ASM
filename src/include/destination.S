/* Reticulum destination hash constants.
 *
 * Upstream Reticulum uses 80-bit name hashes and 128-bit addressable
 * destination hashes.
 */

        .equ DESTINATION_NAME_HASH_SIZE,      10
        .equ DESTINATION_IDENTITY_HASH_SIZE,  16
        .equ DESTINATION_HASH_SIZE,           16
